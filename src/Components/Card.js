import styled from "styled-components";

const Card = styled.div({
    backgroundColor: "white",
    color: "black",
    borderRadius: "16pt",
    padding: "15pt",
    filter: "drop-shadow(0rem 0rem 0.5rem gba(0,0,0,0.75))"
})

export default Card